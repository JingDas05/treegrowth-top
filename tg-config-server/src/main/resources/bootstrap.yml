server:
  port: 8888

#http://localhost:8888/provider/dev 访问本地配置

spring:
  application:
    name: configserver
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri:
        native:
          search-locations: classpath:/
