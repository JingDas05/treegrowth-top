server:
  port: 5555

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    preferIpAddress: true

zuul:
  routes:
    consumer:
      path: /tg/**
      serviceId: consumer
      stripPrefix: true